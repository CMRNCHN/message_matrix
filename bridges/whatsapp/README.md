# mautrix-whatsapp

Run the wizard: `./connect whatsapp`

Then in Element, DM `@whatsappbot:yourdomain` and send `login` to scan the WhatsApp Web QR code.

Docs: https://docs.mau.fi/bridges/go/whatsapp/authentication.html
