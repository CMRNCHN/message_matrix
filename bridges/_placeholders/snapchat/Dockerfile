# Snapchat bridge placeholder

# Community WIP: https://github.com/lalomorales22/snapchat-bridge
# Not wired into docker-compose by default — see bridges/_placeholders/README.md

FROM alpine:3.20
CMD ["echo", "Snapchat bridge placeholder — build from upstream when ready"]
